{
  "scripts": {
    "build": "zola build && npx tailwindcss -i ./styles/main.css -o ./public/style.css -m",
    "serve": "zola serve --interface 0.0.0.0 & npx tailwindcss -i ./styles/main.css -o ./public/style.css --watch && wait",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-jinja-template": "^2.0.0"
  }
}
